{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Constitutional Compliance Validator Input",
  "type": "object",
  "required": ["file_path"],
  "properties": {
    "file_path": {
      "type": "string",
      "description": "Path to the Python file to validate for constitutional compliance",
      "pattern": "\\.py$"
    },
    "strict_mode": {
      "type": "boolean",
      "description": "Whether to run in strict validation mode",
      "default": true
    },
    "constitution_version": {
      "type": "string",
      "description": "Version of the constitution to validate against",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "default": "1.3.5"
    },
    "input_content_type": {
      "type": "string",
      "description": "MIME type of the input content",
      "enum": ["application/json", "text/plain"],
      "default": "application/json"
    },
    "brand_token": {
      "type": "string",
      "description": "Brand configuration token for tone and compliance",
      "pattern": "^[a-zA-Z0-9_-]+$"
    }
  },
  "additionalProperties": false
}